# E2E Flows

Run with Maestro after installing an Android build with app id `com.aiboxcatalog.app`:

```sh
pnpm test:e2e
```

Current flows cover stable app-controlled screens. Camera/gallery system pickers need
device-specific coverage after the Android test fixture is available.
